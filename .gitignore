node_modules/
dist/

# Local SQLite. Whole directory ignored so WAL/journal sidecars never reach a commit.
data/
*.db
*.db-journal
*.db-wal
*.db-shm

.env
.env.*.local
.DS_Store

# Foundry build output and broadcast logs (deploy logs are copied into docs/deployments.md by hand)
contracts/out/
contracts/cache/
contracts/broadcast/

# Next.js frontend (web/)
web/.next/
web/node_modules/
web/out/

# Marketing site (site/)
site/.next/
site/node_modules/
site/out/
site/*.tsbuildinfo

# Submission video (video/) — raw + baked screen recordings are too big for git
video/node_modules/
video/out/
video/public/footage/
